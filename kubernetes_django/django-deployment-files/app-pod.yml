apiVersion: "v1"
kind: Pod
metadata:
  name: django-pod
  labels:
    name: django-pod
    app: django-app

spec:
  containers:
   - name: django
     image: maiempire/django_project-app
     ports:
      - containerPort: 8000


