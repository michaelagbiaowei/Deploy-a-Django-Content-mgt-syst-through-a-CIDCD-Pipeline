---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "django-service"
  namespace: "default"
  labels:
    app: "django"
spec:
  ports:
  - protocol: "TCP"
    port: 80
    targetPort: 8000
  selector:
    app: "django"
  type: "NodePort"
  NodePort: ""
